func abs(x: int,): int {
    let answer: int = 0;

    if (x < 0) {
        answer = -x;
    } else {
        answer = x;
    }

    return answer;
}

func main(): int {
    if (abs(-4,) != 4) {
        return 1;
    }

    return 0;
}