func sum_n(n: int): int {
    let temp: int = n;
    let ans = 0;

    while (temp > 0) {
        ans = ans + temp;
        temp = temp - 1;
    }

    return ans;
}

func main(): int {
    const test = sum_n(10);

    if (test != 55) {
        return 1;
    }

    return 0;
}